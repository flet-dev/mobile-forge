{% set version = "3.13.1" %}

package:
  name: flet-libgeos
  version: '{{ version }}'

build:
  number: 1

source:
  url: http://download.osgeo.org/geos/geos-{{ version }}.tar.bz2

requirements:
  build:
    - cmake
