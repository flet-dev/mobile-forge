package:
  name: orjson
  version: "3.11.9"

build:
  number: 10
  script_env:
    # Force the TARGET sysconfigdata: without it PyO3 reads the build Python's
    # and bakes host paths into the extension.
    _PYTHON_SYSCONFIGDATA_NAME: '{sysconfigdata_name}'
