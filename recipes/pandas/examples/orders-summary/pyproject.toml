[project]
name = "pandas-orders-summary"
version = "1.0.0"
description = "Groups a CSV of orders with pandas and shows the totals."
requires-python = ">=3.11"

dependencies = [
    "flet==0.86.5",
    "pandas==3.0.3",
]

[dependency-groups]
dev = ["flet-cli", "flet-desktop", "flet-web"]

[tool.flet.app]
path = "src"

[tool.flet.cleanup]
package_files = ["pandas/tests"]
