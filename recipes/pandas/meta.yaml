package:
  name: pandas
  version: 2.2.2

requirements:
  build:
    - numpy 2.0.0
  host:
    - meson
    - ninja

build:
  backend-args:
    - -Csetup-args=--cross-file
    - -Csetup-args={MESON_CROSS_FILE}