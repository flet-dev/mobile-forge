package:
  name: astropy
  version: "8.0.0"

# astropy reads astropy/CITATION via a real __file__ path at import.
extract_packages:
  - astropy

build:
  number: 1

requirements:
  host:
    - numpy ^2.0.0

# {% if sdk == 'android' %}
patches:
  - android-cython-cpow.patch
# {% endif %}
