{% set version = "1.1.2" %}

package:
  name: flet-libcrc32c
  version: '{{ version }}'

build:
  number: 11

source:
  url: https://github.com/google/crc32c/archive/refs/tags/{{ version }}.tar.gz

requirements:
  build:
    - cmake

about:
  license: BSD-3-Clause
