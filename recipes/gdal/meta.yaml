package:
  name: gdal
  version: 3.10.0

requirements:
  host:
    - flet-libgdal 3.10.0
# {% if sdk == 'android' %}
    # libgdal links C++; the SWIG-generated _gdal.so loads via dlopen which
    # needs libc++_shared.so on Android. Without this dep the wheel loads
    # with `dlopen failed: library "libc++_shared.so" not found`.
    - flet-libcpp-shared >=27.2.12479018
# {% endif %}

build:
  script_env:
    GDAL_VERSION: 3.10.0
    GDAL_PREFIX: '{platlib}/opt'
    GDAL_CFLAGS: ''
# {% if sdk != 'android' %}
    LDFLAGS: '-undefined dynamic_lookup'
# {% endif %}

patches:
  - config.patch