[project]
name = "numpy-bell-curve"
version = "1.0.0"
description = "Averages uniform random draws with numpy until a bell curve appears."
requires-python = ">=3.10"

dependencies = [
    "flet==0.86.5",
    "numpy==2.4.6",
]

[dependency-groups]
dev = ["flet-cli", "flet-desktop", "flet-web"]

[tool.flet.app]
path = "src"
