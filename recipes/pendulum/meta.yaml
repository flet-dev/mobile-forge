package:
  name: pendulum
  version: 3.2.0

build:
  number: 1
  script_env:
    _PYTHON_SYSCONFIGDATA_NAME: '{sysconfigdata_name}'
# {% if sdk == 'iphonesimulator' %}
    CFLAGS_aarch64-apple-ios-sim: "--target=arm64-apple-ios13.0-simulator"
# {% endif %}

patches:
  - mobile.patch