package:
  name: contourpy
  version: 1.3.0

requirements:
  build:
    - ninja
    - cmake
  host:
    - pybind11
# {% if sdk == 'android' %}
    - flet-libcpp-shared 27.2.12479018
# {% endif %}

build:
  backend-args:
    - -Csetup-args=--cross-file
    - -Csetup-args={MESON_CROSS_FILE}