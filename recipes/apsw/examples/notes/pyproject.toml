[project]
name = "apsw-notes"
version = "1.0.0"
description = "Persistent notes stored in SQLite by apsw."
requires-python = ">=3.10"

dependencies = [
    "flet",
    "apsw",
]

[dependency-groups]
dev = [
    "flet[all]"
]

[tool.flet.app]
path = "src"
