package:
  name: pyxirr
  version: 0.10.6

# Note: the previous `patches: - mobile.patch` directive referenced a
# patch file that no longer exists in the recipe (recipes/pyxirr/patches/
# was removed without updating meta.yaml). pyxirr 0.10.6 builds cleanly
# for both Android and iOS without patches against the current pyo3 /
# maturin toolchain, so the line is dropped rather than reintroducing
# a stale patch.